{
  "name": "altegra-travel-app",
  "version": "0.1.0",
  "private": true,
  "description": "Personal travel expense PWA: record trips and expenses once, copy them into any company's expense system.",
  "type": "module",
  "scripts": {
    "start": "npx --yes http-server -c-1 -p 8080 .",
    "test": "node --test tests/*.test.js"
  }
}
